import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Chat = () => {
  return (
    <View>
      <Text>This is chat screens</Text>
    </View>
  );
};

export default Chat;

const styles = StyleSheet.create({});
